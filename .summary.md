# PLS project handoff / continuation summary

Last updated: **2026-09-03 (Asia/Hong_Kong)**

Repository root: `/media/PM983/Code/PLS`

Git remote: `git@github.com:Media-Foundry/PLS.git`

Active branch: `method/editflow-path-old`

Current HEAD: `33f8e6d7a0759ff20200c645b92ae0b4cc17c107`

Remote state at handoff: `origin/method/editflow-path-old` is also at `33f8e6d`.

This file is the authoritative operational memory for a new agent. Read it
before running anything. The repository has two distinct stories:

1. the **frozen core PLS solubility predictor**, which must not be tuned further;
2. the active methods project, now framed as **Cost-Aware Conformal Gating for
   Cached Derived-Modality Oracles**.

The earlier names EditFlow, Path-OLD, OLD, and intervention distillation refer
to historical stages of the methods branch. They are not the current headline
method.

---

## 1. Non-negotiable rules

### 1.1 Permanent test-set freeze

The repository PLS test split must **never** be evaluated, inferred, scored,
calibrated, inspected, summarized, or used for selection. This includes trying
to infer test membership indirectly. There is no command-line override and no
user authorization bypass.

- Every allowed manifest/report must state `test_evaluated: false`.
- Training entrypoints must hard-fail if `evaluate_test=true`.
- All work described below uses train/validation entities or external user
  sequences only.
- Current test query/evaluation count is exactly **zero**.

### 1.2 Dependency installation contract

For every Python package installation:

1. run `python -m pip install --dry-run ...`;
2. inspect every proposed install, upgrade, downgrade, and removal for conflicts
   with PyTorch/ROCm, NumPy, Biopython, ESM, and the existing environment;
3. only then install with `--no-build-isolation`;
4. abort if the plan changes the core stack; use an isolated environment or a
   compatible version instead;
5. record package versions in the experiment's `environment.json`.

No package installation was needed for the recent local sidework.

### 1.3 Compute authorization

- Local machine: only physical GPUs **0, 1, 2, 3** are authorized for PLS work.
- Do not touch physical GPUs **4, 5, 6, 7**; they have hosted unrelated
  high-memory processes.
- Use explicit `HIP_VISIBLE_DEVICES=<0..3>` and the corresponding guarded CLI
  option where applicable.
- Remote `husrcf@star` is Slurm-managed and has two A100 80GB GPUs. GPU work
  must be submitted via Slurm/GRES. Never bind login-node GPUs directly.
- Star staging root: `/data/husrcf/PLS` (a verified fallback copy exists).
- There are no active project Slurm jobs at this handoff.

### 1.4 Repository hygiene

- Preserve unrelated dirty/untracked files; many old experiment outputs are
  intentionally untracked.
- Do not use destructive git commands.
- Training outputs follow
  `outputs/<experiment-name>+MM-DD-HH-MM/` and should contain configuration,
  environment, TensorBoard/logs, and metrics.
- Checkpoints and large raw prediction arrays are excluded from Git.
- `.codegraph/` is currently absent; skip CodeGraph unless the user later adds
  an index.

---

## 2. Git and freeze state

Recent commits, newest first:

```text
33f8e6d freeze cost-aware conformal protocol v2
92957e3 add cost-aware conformal decision analysis
869d433 add certified cached-oracle decision gating
56cd568 archive multifidelity correction null result
74b08d2 replace exhaustive folding with multifidelity oracle
ea6fb9b scale intervention distillation to 1k SI components
28f1858 distill structural residual over matched sequence teacher
1375819 add exact-pair PLM mutation student
2f20235 record scaled intervention-distillation handoff
dd4c044 automate scaled PLS oracle and intervention students
2dd3050 launch scaled oracle folding on local GPUs
b978e50 allow explicitly authorized local ESM2 devices
```

Core PLS was frozen at:

- exact code revision: `2ff7afbb3ddddcc78d96038ba106134bd8b8a407`;
- tags: `pls-core-pretest-2026-09-01` and
  `core-pls-pretest-2026-09-01`;
- machine-readable freeze:
  `configs/frozen_pretest_state_2026-09-01.json`;
- human-readable freeze:
  `DOCs/frozen_pretest_state_2026-09-01.md`.

Do not merge subsequent method experiments into the frozen core submission.
New predictor architecture ideas belong in a future PLS-v2 project.

### 2.1 Current working tree

The branch HEAD is clean with respect to committed method code, but the worktree
contains many pre-existing untracked historical outputs. Do not delete them.
The two important newly created, still-untracked scoring configs are:

```text
configs/editflow/pls_oracle_score_cost_calibration_exact_v2.json
configs/editflow/pls_oracle_score_cost_calibration_fixed_v2.json
```

They were used successfully for the completed fresh-calibration scoring and
should be reviewed, versioned, and committed together with the calibration
artifact/report. Recent external-sequence sidework directories are also
untracked and are listed in Section 12.

The 2026-09-03 calibration run added three further untracked analysis artifacts:

```text
analysis/editflow_pls_cost_aware_conformal_calibration_v2.json
analysis/editflow_pls_cost_aware_conformal_calibration_v2.md
analysis/editflow_pls_cost_aware_conformal_calibration_v2_expected_cost.json
```

`DOCs/editflow_method.md` was also retitled to the current headline method and
its contribution list reordered so that the cached derived-modality oracle and
conformal decision gating lead, with the path-dependent regret bound and
graph-Sobolev distillation demoted to framing and negative controls.

---

## 3. Frozen core PLS predictor

`configs/validation_selection_v1.json` is the authoritative set of frozen,
metric-specific development recipes. These are heavily optimized strict
validation values, **not** unbiased estimates of unseen-data performance.

| Task | Metric | Frozen strict-validation value |
| --- | --- | ---: |
| PDBSol | AUROC | 0.9046065494 |
| PDBSol | AUPRC | 0.9161942122 |
| PDBSol | MCC | 0.6425591645 |
| PDBSol | SI30-component OOF Brier | 0.1259196054 |
| UESolDS | AUROC | 0.8076030571 |
| UESolDS | AUPRC | 0.8686877438 |
| UESolDS | MCC | 0.4554435366 |
| UESolDS | SI30-component OOF Brier | 0.1744964382 |
| eSOL | Spearman | 0.7720613126 |
| eSOL | Pearson | 0.7642754765 |
| eSOL | RMSE | 0.2096029381 |
| eSOL | MAE | 0.1646387926 |
| eSOL | SI30-component OOF affine RMSE | 0.2094082855 |

The old eSOL affine RMSE `0.2075887243` was fitted and evaluated on the same
validation samples. It is retained only as an explicitly labelled diagnostic.
The frozen deployment affine transformation for the Pearson recipe is:

```text
slope     = 1.1550794511109763
intercept = -0.0570579937680418
```

### 3.1 Core correctness repairs completed before freeze

1. Empty surface-patch sets now return an exact-zero surface representation and
   bypass surface attention/fusion. Historical checkpoints retain their old
   behavior only when explicitly requested for reproducibility.
2. Bidirectional cross-attention applies edge confidence once by default;
   historical v49 checkpoints explicitly retain confidence power 2.
3. Patch spatial message passing excludes self-edges by default; historical v49
   explicitly retains self-edges.
4. Platt, beta, and isotonic calibrators use SI30-component grouped cross-fit.
5. eSOL affine metrics use SI30-component OOF prediction; the fit on all
   validation data is deployment-only.
6. Latent endpoint models are described accurately as scale-unidentified shared
   latent plus endpoint-specific residual models, not an identified intrinsic
   solubility variable.

Audit: `analysis/core_pls_freeze_correctness_audit_2026-09-01.json`.

### 3.2 Frozen data/model seals

```text
strict entity split SHA-256:
1dfc29d4d3a5a333235d72f42335b4b7fd6b869c7522529f4690e48fe9ea764c

strict observation split SHA-256:
987281e064af96f0fccc862017d4cbcb643cb0066081a3a357963a1fccc9c8e7

sequence entity manifest SHA-256:
7ae6549a4c617a11ebf094c4ff04cdea501d9f0a6ff09aac88ab1ddd1077dd47

ESMFold v1 checkpoint SHA-256:
e9a52579027e77d2d2e0a18218e755821f395730e86624cab9413dc117f5ca62

fair-esm version: 2.0.0
```

### 3.3 Scientific interpretation

Core PLS is a strong, computationally expensive scientific oracle. It is a
credible domain-paper system, but continued surface/GVP/MoE/TCN/ensemble tuning
is not the ICLR story. Validation has already served as a large development
dataset. Do not chase another `1e-4` metric improvement.

---

## 4. How the active method direction evolved

The original method hypothesis was optimization-landscape distillation:
distill a full PLM+ESMFold+structure oracle into a sequence-only student while
preserving local mutation effects. The repository then tested several increasingly
careful variants. Negative results are important and must remain visible.

### 4.1 GB1 path/acquisition line: development-only, mostly null

GB1 is now a **method-development landscape**, not a blind confirmatory set.
Earlier evaluation repeatedly inspected the full measured landscape. Do not
repackage any GB1 follow-up as unseen confirmation.

Standard radius-2 normalized novel-design regret AUC (lower is better):

| Acquisition | AUC |
| --- | ---: |
| random | 2.8035 |
| greedy | 2.0757 |
| UCB | 1.9502 |
| Thompson | 2.0416 |
| occupancy-only | **1.8662** |

Important conclusions:

- naive graph-Sobolev/edge loss did not improve Value KD under the same queried
  node budget;
- pure Path did not win its preregistered primary comparison;
- a visually strong fixed 50/50 hybrid result failed replication;
- the first adaptive Path/UCB run had final global R2 `0.4743` but radius-2
  novel-design regret AUC `2.2114`, worse than UCB and occupancy;
- prequential calibration raised final edge Spearman to `0.3671` but radius-2
  regret AUC remained `1.9816`, still worse than occupancy and slightly worse
  than UCB;
- better global edge correlation did not reliably imply lower optimization
  regret.

Key reports:

```text
analysis/editflow_gb1_standard_acquisition_development_matrix_v1.md
analysis/editflow_gb1_adaptive_acquisition_development_matrix_v1.md
analysis/editflow_gb1_prequential_calibrated_path_development_v1.md
```

Do not resume path-weight, entropy-threshold, hybrid-fraction, or uncertainty
calibration sweeps. TrpB was deliberately not opened on this evidence.

### 4.2 Sequence-only intervention students: informative nulls

The first 24-anchor/384-edge PLS PoC established a large structure/fusion
mutation contribution:

```text
mean |full-oracle mutation delta|       = 0.2090
mean |matched sequence-only delta|      = 0.0487
mean |structure/fusion residual delta|  = 0.1911
```

Full-vs-sequence mutation Spearman was only about `0.305`, with approximately
`61.5%` sign agreement. The structure signal persisted among high-pLDDT
anchors. Thus the expensive derived modality is scientifically nontrivial.

However, students did not preserve the edit field:

- raw-token Value KD had edge Spearman `0.1171`, sign `0.5391`;
- adding edge KD reduced edge Spearman to `0.0777`;
- a frozen-ESM2 potential improved Pearson/RMSE but global edge Spearman remained
  around `0.1028` in the small PoC;
- direct-delta and cycle-consistent variants were also null;
- cycle supervision used 1,900 commuting squares and zero additional teacher
  labels, so its failure was not a query-budget artifact.

In the scaled 128-train/32-validation experiment (2,560 edges):

```text
raw-token potential edge Spearman       = 0.1283
frozen-PLM potential edge Spearman      = 0.3297
exact-pair direct-delta edge Spearman   = 0.2887
structural-residual potential:
  Pearson 0.4159, Spearman 0.3727, sign 0.6563, RMSE 0.6208
matched sequence branch:
  Spearman 0.4279, sign 0.6855
```

The residual model improved magnitude fidelity but did not beat the matched
sequence branch on rank/sign. This line is not the current method.

Reports:

```text
analysis/editflow_pls_oracle_poc_v1.md
analysis/editflow_pls_student_poc_v1.md
analysis/editflow_pls_plm_student_poc_v1.md
analysis/editflow_pls_student_scale_v1.md
```

---

## 5. Multi-fidelity discovery: cached parent structure

The decisive empirical observation was that mutation-specific refolding is
often unnecessary for a local single-edit decision even though structural
context itself matters strongly.

Define:

```text
H_j = F(h(mutant_j), G(mutant_j))   # exact-refold/high-fidelity oracle
L_j = F(h(mutant_j), G(parent))     # fixed-parent/cached-context oracle
```

On the complete 2,560-edge ablation:

| Scope | Edge Pearson | Edge Spearman | Sign accuracy | Macro Kendall | Top-5 recall |
| --- | ---: | ---: | ---: | ---: | ---: |
| train, 2,048 edges | 0.6925 | 0.6771 | 0.7773 | 0.5681 | 0.6906 |
| validation, 512 edges | 0.7441 | 0.7692 | 0.8281 | 0.6042 | 0.7125 |
| all, 2,560 edges | 0.7059 | 0.6967 | 0.7875 | 0.5753 | 0.6950 |

Node-level Pearson/Spearman were approximately `0.9967/0.9964`. This yields the
important general observation:

> Near-perfect value approximation does not imply near-perfect intervention
> approximation, but cached expensive context can still preserve enough local
> ranking to drive a useful high-to-low fidelity cascade.

The 1k-component protocol contains:

- 1,024 unique train SI30 components;
- 128 unique validation SI30 components;
- one anchor and 16 mutations per component;
- 18,432 dense fixed-parent edges;
- exact calibration subset: 1,024 train edges plus all 2,048 validation edges;
- 3,072 exact mutant structures total, 822 already cached and 2,250 newly folded;
- zero fold failures and zero test queries.

Report: `analysis/editflow_pls_fixed_parent_oracle_scale_v1.md`.

### 5.1 Global correction and discrepancy selection failed

Grouped OOF selection on 1,024 component-unique train edges retained raw
fixed-parent over affine, ridge residual, and nonlinear residual correction.
On 2,048 exact validation edges, raw fixed-parent achieved:

```text
Pearson       0.7481
Spearman      0.6896
sign          0.7842
RMSE          0.3414
macro Kendall 0.5850
top-5 recall  0.6844
```

The discrepancy predictor had Spearman only `0.2013`. Selecting exact refolds
by predicted absolute discrepancy failed to beat random at 1%, 5%, 10%, or 20%
budgets. Affine/ridge/nonlinear correction sweeps and per-anchor 1/2/4-probe
scalar attenuation are closed unless a genuinely new hypothesis is proposed.

The non-deployable perfect-discrepancy selector showed large theoretical
headroom, but the learned gate did not capture it. Do not claim selective
discrepancy refolding works.

Report: `analysis/editflow_pls_multifidelity_correction_v1.md`.

---

## 6. Current headline method: conformal decision gating

The method no longer tries to reconstruct the exact oracle globally. It asks
which candidates must receive expensive exact evaluation to make a controlled
decision.

For one parent/component with candidates `j=1..m`, define exact decision regret
of a queried set `C`:

```text
R(C; x) = max_j H_j - max_{j in C} H_j
```

For epsilon-optimal decisions:

```text
J_epsilon(x) = {j : H_j >= max_k H_k - epsilon}
```

The original absolute-margin nonconformity was:

```text
S(x) = max_j L_j - L_(exact-best)
```

and the candidate set was all mutations within the conformal margin of the
cached-oracle top score. With SI30 component as the exchangeability unit,
split-conformal calibration provides marginal control of excluding an
epsilon-optimal exact decision. It is **not** a deterministic per-protein
certificate.

### 6.1 Frozen v1 held-out proof of principle

The v1 threshold was frozen before the 64-component held-out exact folds:

```text
alpha  = 0.1
margin = 0.2325940132
```

The 64 anchors were from 64 SI30 components disjoint from all 1,152 prior oracle
components, one anchor per component. All 1,024 exact folds completed with zero
failures.

| Policy | Mean exact queries | Exact-best coverage | Mean regret | Max regret |
| --- | ---: | ---: | ---: | ---: |
| Top-4 | 4.00/16 | 82.81% | 0.1107 | 4.6084 |
| Top-8 | 8.00/16 | 93.75% | 0.0952 | 4.5960 |
| conformal margin | 9.70/16 | 93.75% | 0.0271 | 1.3061 |
| rank-12 | 12.00/16 | 98.44% | 0.0204 | 1.3061 |
| exhaustive | 16/16 | 100% | 0 | 0 |

Margin had the same four exact-best misses as Top-8 but reduced
failure-conditional mean regret from `1.5226` to `0.4336`. Its principal value
was milder catastrophic misses, not strictly better coverage.

The exact fold campaign consumed 3,060.3 ESMFold GPU-seconds and 890.9 seconds
of measured four-GPU wall time. Replaying the margin selection showed:

```text
query-count saving                 39.36%
measured ESMFold inference saving  26.91%
replayed four-GPU wall saving      22.2% (counterfactual replay, not measured)
```

Rank-12 used only about 3.1% more measured ESMFold GPU time than margin while
improving coverage/regret, showing why query count is not an adequate cost.

Reports:

```text
analysis/editflow_pls_decision_gating_confirmatory_v1.md
analysis/editflow_pls_decision_gating_confirmatory_v1_cost_audit.md
analysis/editflow_pls_decision_gating_confirmatory_v1_miss_audit.md
```

### 6.2 Quantile implementation detail

The v1 result used a conservative one-order-higher quantile due to NumPy
`method="higher"` indexing. That result is frozen and must not be recomputed.
New code uses the direct split-conformal order statistic:

```python
k = ceil((n + 1) * (1 - alpha))
q = sorted_scores[min(k, n) - 1]
```

A separate legacy helper reproduces the v1 threshold exactly.

---

## 7. Cost-aware epsilon-optimal formulation

The active optimization problem is:

```text
minimize expected measured high-fidelity compute cost
subject to P(R <= epsilon) >= 1 - alpha
```

The new cost-scaled score uses a frozen predicted runtime `c_hat(x)` and a
normalization reference `c0` learned before fresh calibration:

```text
a_gamma(x) = (c_hat(x) / c0)^(-gamma)

S_(epsilon,gamma)(x) =
    min_{j in J_epsilon(x)} (max_k L_k - L_j) / a_gamma(x)
```

After calibrating `q`, the candidate set contains candidates satisfying the
corresponding scaled margin inequality.

### 7.1 Frozen runtime cost model

Config: `configs/editflow/pls_esmfold_runtime_cost_model_v1.json`

SHA-256:
`1f6d6ab1773e6af8daced429e32d71f15c3662ee2c011327648d5b6ae437e7fb`

The label-free monotone runtime model was fitted to 5,814 marginal timings from
20 homogeneous ROCm ESMFold shards. First successful sequence per process was
excluded as warm-up; startup cost remains separate.

```text
group-by-shard CV Spearman  0.9339
median absolute error       0.0641 s
median absolute percentage  2.97%
```

Report: `analysis/editflow_pls_runtime_cost_model_v1.md`.

### 7.2 Development-only policy selection

Policy shape was selected using the old 128 anchors spanning 89 SI30
components. The 64-component v1 confirmatory set was not used for v2 selection.

Runtime-cost development results for `gamma=1`:

```text
epsilon=0.0: component exact coverage 0.9213,
             predicted cost fraction 0.6514

epsilon=0.2: component epsilon coverage 0.9213,
             predicted cost fraction 0.3452
```

`epsilon=0.2` equals about `1.021` development mutation-effect IQR. Therefore:

- frozen primary: `epsilon=0`, `gamma=1`;
- frozen secondary: `epsilon=0.2`, `gamma=1`.

Do not tune epsilon, gamma, runtime normalization, score family, or length
strata using fresh calibration or confirmatory labels.

Report: `analysis/editflow_pls_epsilon_runtime_cost_development_v2.md`.

---

## 8. Frozen protocol v2

Authoritative config:
`configs/editflow/pls_cost_aware_conformal_protocol_v2.json`.

Commit `33f8e6d` froze the protocol **before fresh exact calibration scores**.

### 8.1 Primary policy

```text
policy_id: exact_best_runtime_gamma1
role:      primary
alpha:     0.1
epsilon:   0.0
gamma:     1.0
risk:      exact high-fidelity decision regret equals zero
```

### 8.2 Secondary policy

```text
policy_id: tolerant_runtime_gamma1
role:      secondary
alpha:     0.1
epsilon:   0.2 raw PLS-teacher logit
gamma:     1.0
risk:      high-fidelity decision regret <= 0.2
```

The `0.2` tolerance is an oracle-score utility tolerance, not an experimentally
calibrated change in solubility.

### 8.3 Fresh data partitions

There are two disjoint, label-blind, train-only sets:

| Set | SI30 components | Anchors/component | Mutations/anchor | Prior-component overlap |
| --- | ---: | ---: | ---: | ---: |
| fresh calibration | 128 | 1 | 16 | 0 |
| fresh confirmatory | 128 | 1 | 16 | 0 |

They have zero overlap with one another and with all 1,216 previously used
oracle components.

Fresh calibration manifest:

```text
benchmark/generated/pls_editflow_oracle_manifest_cost_calibration_v2.json
SHA-256 ed03c0d5261850e2296b4640f0dc24deb707e62ed827b78569c5c062568b0dd9
```

Fresh confirmatory manifest:

```text
benchmark/generated/pls_editflow_oracle_manifest_cost_confirmatory_v2.json
SHA-256 dda1f9f7b5acff8cc17f734d4c1ba921f88a936516f90bbac5ae3631d8b03195
```

---

## 9. Fresh calibration v2: exact current status

This section supersedes the stale pre-launch statement in the previous summary.

### 9.1 Completed

Fresh calibration computation is now complete through exact/fixed oracle
scoring:

- 128 anchors + 2,048 exact single mutants = **2,176 nodes**;
- all 2,048 exact mutant ESMFold jobs completed on physical GPUs 0--3;
- four shards, 512 mutants per shard;
- zero fold failures;
- all 2,176 exact V4 raw features complete;
- compact 152D structure caches complete for PDBSol and eSOL statistics;
- kNN-16 geometry, 8x3 vector channels, and surface patch components complete;
- ESM2 mean, multilayer, residue fp16, and frozen train-PCA projections complete;
- exact-parent and fixed-parent feature trees each cover all 2,176 nodes;
- both coherent frozen-teacher scoring passes completed in float32.

Artifact root:

```text
artifacts/oracles/pls_editflow_cost_calibration_v2
```

Approximate size at handoff: **2.3 GB**.

Important subtrees:

```text
exact/esmfold
exact/structure_v4_raw
exact/structure_v4_compact
exact/structure_v4_geometry
exact/structure_v4_vectors
exact/surface_patch_components
exact/scores_fp32_a

fixed_parent/structure_v4_raw
fixed_parent/structure_v4_compact
fixed_parent/structure_v4_geometry
fixed_parent/structure_v4_vectors
fixed_parent/surface_patch_components
fixed_parent/scores_fp32_a

esm2_mean
residue_esm2_raw
residue_esm2_pca
logs
```

### 9.2 Completed score reports

Exact-refold scoring:

```text
artifacts/oracles/pls_editflow_cost_calibration_v2/exact/scores_fp32_a/oracle_logits.npz
artifacts/oracles/pls_editflow_cost_calibration_v2/exact/scores_fp32_a/oracle_report.json

queries             2176
checkpoint epoch    5
checkpoint SHA-256  d52742798af581d44c1dda59ae572e707ad14346c12f97aaaddb72ca6758e25f
precision           float32
full logit min/mean/max  -9.625996 / -0.407466 / 9.097602
test_evaluated      false
```

Fixed-parent scoring:

```text
artifacts/oracles/pls_editflow_cost_calibration_v2/fixed_parent/scores_fp32_a/oracle_logits.npz
artifacts/oracles/pls_editflow_cost_calibration_v2/fixed_parent/scores_fp32_a/oracle_report.json

queries             2176
checkpoint epoch    5
checkpoint SHA-256  d52742798af581d44c1dda59ae572e707ad14346c12f97aaaddb72ca6758e25f
precision           float32
full logit min/mean/max  -9.544584 / -0.395723 / 9.132499
test_evaluated      false
```

Both use the same manifest SHA-256:
`b5329b2abdfa6b165962709fbf8c02644e00f2bebcce3e2cb4a1d26462e9b3cd`.
Both use a coherent single checkpoint and the matched same-checkpoint sequence
ablation. The sequence-only logit summary is identical in both reports, as
expected.

### 9.3 Completed: frozen fresh quantiles

Fresh split-conformal quantiles were calibrated once on 2026-09-03 with the
frozen protocol and without altering any policy parameter.

Command actually run:

```bash
PYTHONPATH=src /home/pc/anaconda3/envs/BIO/bin/python \
  -m pls.training.calibrate_editflow_pls_cost_aware \
  --protocol configs/editflow/pls_cost_aware_conformal_protocol_v2.json \
  --output analysis/editflow_pls_cost_aware_conformal_calibration_v2.json
```

Frozen quantiles from 128 fresh calibration components, order statistic
`ceil((n + 1) * (1 - alpha))` with `n = 128`, giving rank 117:

| Policy | Role | epsilon | gamma | Quantile |
| --- | --- | ---: | ---: | ---: |
| `exact_best_runtime_gamma1` | primary | 0.0 | 1.0 | 0.3915251122391252 |
| `tolerant_runtime_gamma1` | secondary | 0.2 | 1.0 | 0.1390406840591033 |

Expected candidate cost projected on the calibration components themselves:

| Policy | Mean exact queries | Query fraction | Predicted GPU-cost fraction |
| --- | ---: | ---: | ---: |
| `exact_best_runtime_gamma1` | 10.52/16 | 0.6577 | 0.6162 |
| `tolerant_runtime_gamma1` | 4.31/16 | 0.2695 | 0.2470 |

In-sample coverage is `0.9141` for both policies, which is exactly rank 117 of
128 and is therefore self-consistent by construction rather than evidence. These
components produced the quantiles; their coverage must never be reported as
confirmatory. The observed cost fractions track the development projections of
`0.6514` and `0.3452` closely, with the tolerant policy landing cheaper.

Inputs were validated before calibration: manifest, query-plan, runtime-model,
and development-report SHA-256 all match the frozen protocol; all 2,176 nodes are
train-only with unique digests; both score files are dense, ordered, finite, and
agree with the manifest node order; both cite the same frozen teacher checkpoint
`d527...e25f`; and the matched sequence-only ablation is bit-identical across the
two fidelities. Fresh-calibration node Pearson/Spearman are `0.9982/0.9975` while
edge Spearman is `0.6292` with sign accuracy `0.7651`, reproducing the central
cached-oracle observation on unseen components.

The predefined length strata already show the mechanism the protocol was written
to detect. Under the primary `gamma=1` policy, in-sample component coverage falls
monotonically with protein length while mean regret rises:

| Length | Components | Mean queries | Predicted cost fraction | In-sample coverage | Mean regret |
| --- | ---: | ---: | ---: | ---: | ---: |
| <=106 | 20 | 9.60 | 0.6079 | 0.9500 | 0.0147 |
| 107-148 | 27 | 13.96 | 0.8699 | 0.9630 | 0.0063 |
| 149-219 | 42 | 10.38 | 0.6318 | 0.9048 | 0.0288 |
| >=220 | 39 | 8.77 | 0.5556 | 0.8718 | 0.0508 |

This is the intended behaviour of the cost scale `a_gamma(x) = (c_hat/c0)^(-gamma)`
and also its principal risk: a long candidate is expensive, so its threshold
tightens, fewer candidates are retained, and conditional coverage degrades. The
secondary tolerant policy does not show the trend, staying between `0.9000` and
`0.9259` across all four bins, because the epsilon tolerance absorbs it. Do not
respond by tuning gamma; the policy family is frozen. Carry this stratum table
into the confirmatory report as the predefined conditional diagnostic, and state
plainly that the finite-sample guarantee is marginal over SI30 components and
does not cover the long-protein bin separately.

Note on hashes: `oracle_manifest_sha256` inside a score report is the SHA-256 of
the **teacher oracle config** `configs/oracles/pls_gvp_bidir_v49_teacher_v1.json`,
not of the landscape manifest. Do not compare it against
`fresh_calibration.manifest_sha256`.

Artifacts:

```text
analysis/editflow_pls_cost_aware_conformal_calibration_v2.json
analysis/editflow_pls_cost_aware_conformal_calibration_v2.md
analysis/editflow_pls_cost_aware_conformal_calibration_v2_expected_cost.json
```

The full suite was rerun after the artifacts were added: 163 tests, all
successful, with 1 authorized-GPU smoke test skipped.

### 9.4 Completed: confirmatory cached-parent oracle

The cached-parent confirmatory oracle was built on 2026-09-03 with **zero mutant
folds**. All 128 confirmatory anchors already had exact V4 tensors in the
repository cache, so every one of the 2,176 confirmatory structure records is a
hardlink of its anchor tensor under the mutant's own exact-sequence key.

Stages, all on authorized GPUs 0--3 where a GPU was used at all:

| Stage | Result |
| --- | --- |
| fixed-parent V4 materialization | 2,176 hardlinked, 0 copied, 0 folds |
| compact / kNN-16 geometry / vectors / surface patches | 370,583 residues, 2,176 entities |
| mean ESM2, residue ESM2, residue PCA | 2,176/2,176 complete |
| cached-parent oracle scoring | 2,176 queries, float32, train-only |

Build script: `script/tmux_editflow_cost_confirmatory_v2_fixed_parent.sh`, which
refuses to run if any confirmatory exact-fold tree already exists.
Scoring config: `configs/editflow/pls_oracle_score_cost_confirmatory_fixed_v2.json`.
Artifact root `artifacts/oracles/pls_editflow_cost_confirmatory_v2`, 1.3 GB.

### 9.5 Completed: gated selection, preregistered

The frozen quantiles were applied once through
`pls.training.select_editflow_pls_cost_aware`, reading only the cached-parent
oracle and label-free covariates. The script hard-refuses if a confirmatory
exact-fold tree exists or if exact scores are supplied.

| Policy | Role | Selected | Mean queries | Query fraction | Predicted GPU-cost fraction |
| --- | --- | ---: | ---: | ---: | ---: |
| `exact_best_runtime_gamma1` | primary | 1245/2048 | 9.73/16 | 0.6079 | 0.6009 |
| `tolerant_runtime_gamma1` | secondary | 498/2048 | 3.89/16 | 0.2432 | 0.2305 |

The primary policy's 9.73/16 sits between the calibration-set projection of
10.52/16 and the frozen v1 confirmatory result of 9.70/16. Per-anchor spread is
wide: 18 components retain a single candidate and 34 retain all 16, with a median
of 12. Unlike the calibration set, selected counts across the predefined length
strata are flat here, ranging only 8.83 to 10.62, so the in-sample long-protein
undercoverage trend did not reappear as a selection-size trend. Whether it
reappears as a coverage trend cannot be known until the selected folds run.

The fold plan restricted to the primary selection is
`benchmark/generated/pls_editflow_oracle_query_plan_cost_confirmatory_v2_selected.json`,
1,245 queries across 4 shards with a maximum-to-minimum predicted shard cost
ratio of 1.0013 and roughly 771 predicted GPU seconds per shard. Like the other
protocol v2 preregistration files it is force-added despite the
`benchmark/generated/` ignore rule.

Artifacts:

```text
analysis/editflow_pls_cost_aware_confirmatory_v2_selection.json
analysis/editflow_pls_cost_aware_confirmatory_v2_selection.md
benchmark/generated/pls_editflow_oracle_query_plan_cost_confirmatory_v2_selected.json
```

### 9.6 Not yet completed

No confirmatory mutant has been folded. Selected-stage measured cost, coverage,
regret, and every confirmatory endpoint remain unknown and must not be estimated
from cached-parent scores.

## 10. Confirmatory v2: mandatory execution order

This ordering is part of the scientific protocol and must not be changed:

1. Build exact-sequence PLM features and **cached-parent/fixed-parent** structure
   features for all confirmatory nodes. Do not fold confirmatory mutants.
2. Score every confirmatory candidate with the fixed-parent oracle.
3. Apply the frozen fresh-calibration quantiles for both frozen policies.
4. Persist a selected-candidate manifest and selection report.
5. Commit and push that selection **before any confirmatory mutant refold**.
6. Fold and score only selected mutants; record real per-query GPU seconds,
   selected-stage wall time, CPU feature/scoring time, and deployment outputs.
7. Freeze the selected-only deployment report.
8. Only after that freeze, fold unselected candidates for retrospective
   exhaustive coverage/regret/cost denominators.

Never precompute all confirmatory mutant structures before the selection is
persisted. Doing so would destroy the strongest operational evidence for a
budgeted deployment.

Primary endpoint:

```text
measured selected-stage ESMFold inference GPU-seconds
----------------------------------------------------
retrospective exhaustive ESMFold inference GPU-seconds
```

subject to component-level marginal control of zero exact decision regret at
`alpha=0.1` for the primary policy.

Secondary endpoints:

- actual end-to-end selected-stage wall time;
- CPU feature/geometry/vector/patch/scoring time;
- exact query fraction;
- epsilon=0.2 component coverage;
- exact-best coverage;
- mean regret;
- failure-conditional mean regret;
- CVaR95;
- maximum regret.

Predefined descriptive strata (not separate guaranteed groups):

```text
length quartiles: <=106, 107--148, 149--219, >=220
parent pLDDT boundaries: 0.7, 0.8, 0.9
```

Report coverage, mean queries, measured GPU seconds, and mean regret per stratum
to detect cost-aware undercoverage on long/hard proteins. The finite-sample
guarantee remains marginal across SI30 components.

---

## 11. Claims that are and are not supported

### Supported

- PLS is a meaningful expensive sequence+predicted-structure oracle.
- Structural/fusion context contributes strongly to local mutation effects.
- Mutation-specific refolding is often less important than evaluating the
  mutant sequence in the parent's structural context.
- Cached-parent scoring preserves useful single-edit ranking at far lower fold
  cost.
- Near-perfect node correlation can coexist with materially lower edit ranking.
- Global residual correction, naive edge KD, and naive discrepancy selection
  do not solve the problem.
- A frozen component-level conformal margin transferred well to 64 new SI30
  components and reduced severe misses versus Top-8.
- Query count materially overstates real GPU savings.
- Cost-aware scaling showed a promising development Pareto improvement and now
  has a clean, fresh calibration/confirmatory protocol.

### Not yet supported

- that v2 primary/secondary policies achieve 90% coverage on fresh
  confirmatory components;
- that predicted runtime savings transfer to actual selected-stage GPU savings;
- conditional coverage for every protein, length bin, or pLDDT bin;
- deterministic certification of the exact best mutation;
- generalization to a second independent scientific oracle;
- superiority over all conformal cascades or multi-fidelity best-arm methods;
- that Path-OLD, edge distillation, correction, or discrepancy selection is a
  headline positive method result.

Use the phrase **component-level marginal risk control** or **marginally valid
conformal decision set**, not “certified best mutation for each protein.”

---

## 12. External-sequence sidework completed on 2026-09-02

These are deployment-style predictions using the three frozen endpoint recipes.
They are external sequences, not repository test examples. Every inference was
run twice with exactly zero numerical replay difference and
`test_evaluated=false`.

Endpoint semantics:

- eSOL: continuous cell-free soluble fraction, affine deployment estimate;
- PDBSol: calibrated probability for the PDBSol/ProtSolM-style binary endpoint;
- UESolDS: calibrated probability for the expression/solubility binary endpoint.

Probabilities can be overconfident for unusual constructs and are not literal
experimental success rates.

### 12.1 Original choA and chiA sequences

Directory:
`outputs/sidework_seq_frozen_endpoints+09-01-17-52`

| Sequence | Length | eSOL | PDBSol | UESolDS |
| --- | ---: | ---: | ---: | ---: |
| choA | 453 | 0.325136 | 0.390361 | 0.249993 |
| chiA | 277 | 0.303890 | 0.746686 | 0.379085 |

### 12.2 Short peptide `RQIKIWFQNRRMKWKK`

Directory:
`outputs/sidework_peptide_frozen_endpoints+09-02-20-35`

```text
length                  16
eSOL                    0.674243
PDBSol                  0.999341
UESolDS                 0.982793
ESMFold mean pLDDT      80.64
median pLDDT            82.47
```

This peptide is far outside the training length distribution. The directional
high-solubility signal is exploratory; the near-one probabilities are not
calibrated OOD confidence.

### 12.3 MBP+chiA-by-sequence

The user initially called this MBP+choA, but the C-terminal supplied sequence
was exactly the earlier chiA sequence. It is recorded by sequence fact as
`MBP_chiA_by_sequence`.

Directory:
`outputs/sidework_mbp_chia_frozen_endpoints+09-02-21-00`

```text
length                  683
eSOL                    0.430605
PDBSol                  0.986754
UESolDS                 0.996622
ESMFold mean pLDDT      79.92
median pLDDT            87.53
fraction pLDDT >= 70    0.8023
fraction pLDDT < 50     0.1215
```

### 12.4 MBP+choA

Directory:
`outputs/sidework_mbp_choa_frozen_endpoints+09-02-21-30`

```text
length                  859
eSOL                    0.417453
PDBSol                  0.961487
UESolDS                 0.993356
ESMFold mean pLDDT      81.28
median pLDDT            89.67
fraction pLDDT >= 70    0.8580
fraction pLDDT < 50     0.1257
```

Relative to choA alone, all three frozen endpoint predictions increased, most
strongly the two binary expression/solubility endpoints.

### 12.5 Human CHIT1 constructs with and without MBP

Directory:
`outputs/sidework_chit1_mbp_comparison+09-02-22-10`

Assembly used exactly the supplied residues. The “no MBP” construct's header
said `6xHis+MBP+TEV+G4S2`, but the supplied segment contained only
`HHHHHHGGGGSGGGGS`; no missing MBP residues were invented.

| Construct | Length | eSOL | PDBSol | UESolDS | Mean pLDDT | Median pLDDT |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| CHIT1 no MBP | 497 | 0.409009 | 0.994121 | 0.649388 | 90.00 | 95.70 |
| CHIT1 with MBP | 896 | 0.480880 | 0.990743 | 0.978412 | 78.63 | 83.45 |

MBP increased eSOL by `0.07187` and UESolDS probability by `0.3290`. PDBSol was
saturated near one for both constructs, so its `-0.0034` difference is not
meaningful. The lower whole-chain pLDDT with MBP is plausibly driven by flexible
linkers and uncertain inter-domain arrangement, not necessarily poor folding of
each domain.

Each sidework directory contains `predictions.json`, replay files, input
provenance, ESMFold PDB(s), extracted V4/GVP/surface features, and logs.

---

## 13. Environment and implementation notes

- Main local environment used for current inference:
  `/home/pc/anaconda3/envs/BIO`.
- Biopython and the complete PLS/ESM/V4 dependency stack are available there.
- Official ESMFold loading uses an audited compatibility shim:
  exactly four OpenFold IPA projection keys are remapped and exactly five known
  legacy buffers ignored; any other mismatch hard-fails.
- Local V4 source root:
  `/home/pc/Code/BIO/protein`.
- The V4 representation includes 152 scalar features in compact caches plus
  separate 8x3 vector channels; do not describe current GVP input as only 152D.
- Canonical scientific-oracle scoring for method work uses coherent checkpoint
  SHA `d527...e25f`, raw logits, and float32 inference. BF16 exhibited materially
  larger replay drift in earlier audits.
- The last fully recorded suite at commit `33f8e6d` ran 163 tests successfully
  with one authorized-GPU smoke test skipped. Run targeted tests after the fresh
  calibration artifact is added, then rerun the full suite before commit.

---

## 14. Immediate next actions

The selection is preregistered. Do these in order:

1. Fold **only** the 1,245 planned selected mutants on authorized GPUs 0--3 using
   `benchmark/generated/pls_editflow_oracle_query_plan_cost_confirmatory_v2_selected.json`.
   Record real per-query GPU seconds, selected-stage wall time, and CPU
   feature/scoring time. Never widen the plan.
2. Build exact V4 features and score the selected mutants with the same frozen
   teacher in float32, writing to
   `artifacts/oracles/pls_editflow_cost_confirmatory_v2/exact_selected/`.
3. Freeze the selected-only deployment report before computing anything else.
4. Only after that freeze, fold the 803 unselected candidates into
   `exact_full/` for retrospective coverage, regret, and cost denominators.
5. Report the primary endpoint, measured selected-stage ESMFold GPU-seconds over
   retrospective exhaustive GPU-seconds, subject to component-level marginal
   control of zero exact decision regret at `alpha=0.1`.
6. Report the secondary endpoints and the predefined length and parent-pLDDT
   strata, stating plainly that the guarantee is marginal over SI30 components.

Already completed on 2026-09-03 and requiring no repetition: calibration input
validation, the single frozen calibration run, calibration artifacts and report,
the confirmatory cached-parent build, the gated selection, its fold plan, and two
full test-suite runs.

## 15. One-paragraph project narrative

PLS began as a high-performing sequence-plus-predicted-structure solubility
predictor and is now frozen as an expensive scientific oracle. Attempts to
distill its mutation landscape into sequence-only potentials, direct-delta
models, cycle-consistent fields, or path-aware acquisition exposed a robust
failure mode: good value or global edge fidelity does not guarantee useful
optimization decisions. A stronger empirical structure then emerged: for local
single mutations, recomputing the mutant PLM representation while reusing the
parent's expensive predicted structure preserves mutation ranking surprisingly
well, despite structure itself contributing strongly. Global correction and
absolute-discrepancy selection failed, so the method shifted from predicting the
exact oracle to buying exact refolds only when needed for a decision. A frozen
component-safe conformal margin transferred to new SI30 components, reduced
catastrophic misses, and saved compute, but query count overstated real GPU
savings. The current paper therefore studies cost-aware, epsilon-optimal
conformal gating of cached derived-modality oracles, with a rigorously frozen
fresh-calibration/fresh-confirmatory protocol. Fresh calibration folding, oracle
scoring, and split-conformal quantile calibration are now complete; building
cached-parent confirmatory scores and persisting the gated selection is the
immediate next step, and confirmatory mutant folding stays forbidden until that
selected set is frozen and committed.
