# PLS project continuation summary

Last updated: 2026-09-01 (Asia/Hong_Kong)

## Current state

Core PLS predictor development is frozen. Do not launch new architecture,
hyperparameter, hard-negative, ensemble-weight, or calibration-family sweeps for
the frozen submission. New predictor ideas belong on a future `PLS-v2` branch;
general methods work continues on `method/editflow-path-old`.

The repository is currently on `method/editflow-path-old`, which contains both
the frozen core predictor and EditFlow/Path-OLD research code. The core freeze is
sealed by:

- `configs/frozen_pretest_state_2026-09-01.json`;
- `DOCs/frozen_pretest_state_2026-09-01.md`;
- frozen core code revision `2ff7afbb3ddddcc78d96038ba106134bd8b8a407`;
- immutable exact-code tag `pls-core-pretest-2026-09-01`.

The test split is permanently prohibited: never evaluate, infer, score,
calibrate, inspect, or use it for selection. Trainers hard-fail on
`evaluate_test=true`; there is no override.

## Frozen strict-validation recipes

`configs/validation_selection_v1.json` is the authoritative metric-specific
selection file. These are heavily optimized development metrics, not estimates
of performance on unseen data.

| task | metric | frozen strict-validation value |
| --- | --- | ---: |
| PDBSol | AUROC | 0.9046065494 |
| PDBSol | AUPRC | 0.9161942122 |
| PDBSol | MCC | 0.6425591645 |
| PDBSol | SI30-component OOF Brier | 0.1259196054 |
| UESolDS | AUROC | 0.8076030571 |
| UESolDS | AUPRC | 0.8686877438 |
| UESolDS | MCC | 0.4554435366 |
| UESolDS | SI30-component OOF Brier | 0.1744964382 |
| eSOL | Spearman | 0.7720613126 |
| eSOL | Pearson | 0.7642754765 |
| eSOL | RMSE | 0.2096029381 |
| eSOL | MAE | 0.1646387926 |
| eSOL | SI30-component OOF affine RMSE | 0.2094082855 |

The old eSOL value `0.2075887243` is validation-fitted affine RMSE and is kept
only as a labelled diagnostic. The frozen deployment affine mapping for the
Pearson recipe is slope `1.1550794511`, intercept `-0.0570579938`.

## Freeze correctness repairs

Completed before the core freeze:

1. Empty surface-patch sets now produce an exact zero surface vector and
   auxiliary logit, and bypass patch/residue and pooled fusion. The selected PDB
   surface candidate had 0 empty sets among 6,429 validation entities, so stored
   predictions are unaffected.
2. Sparse bidirectional cross-attention applies edge confidence once by default.
   Historical v49 checkpoints explicitly request power 2 for reproducibility.
3. Patch spatial message passing excludes self-edges by default. Historical v49
   checkpoints explicitly retain self-edges for reproducibility.
4. Platt, beta, and isotonic calibration cross-fit by indivisible SI30 component
   using `StratifiedGroupKFold`.
5. eSOL affine calibration reports five-fold SI30-component OOF metrics; the
   all-validation fit is deployment-only.
6. Latent endpoint models are described as scale-unidentified shared-latent
   models. With assay residuals enabled they are not claimed to be a strict
   monotonic intrinsic-solubility variable.

The machine-readable audit is
`analysis/core_pls_freeze_correctness_audit_2026-09-01.json`.

## Frozen data and feature seals

- strict entity split SHA-256:
  `1dfc29d4d3a5a333235d72f42335b4b7fd6b869c7522529f4690e48fe9ea764c`;
- strict observation split SHA-256:
  `987281e064af96f0fccc862017d4cbcb643cb0066081a3a357963a1fccc9c8e7`;
- sequence entity manifest SHA-256:
  `7ae6549a4c617a11ebf094c4ff04cdea501d9f0a6ff09aac88ab1ddd1077dd47`;
- ESMFold v1 checkpoint SHA-256:
  `e9a52579027e77d2d2e0a18218e755821f395730e86624cab9413dc117f5ca62`;
- fair-esm version: `2.0.0`.

The full split hashes seal test membership transitively. No test-specific list or
metric was read to produce the freeze manifest.

## Interpretation of core PLS

Core PLS is a strong, expensive scientific oracle and a viable domain paper,
but it is no longer being developed as the main ICLR novelty. Surface/GVP,
Physchem MoE, hard-pair ranking, and latent endpoint candidates contribute small
ensemble diversity rather than a dominant architecture breakthrough.

The latent endpoint evidence supports the name “shared latent + endpoint-specific
residual model”; do not call the learned scalar identifiable intrinsic
solubility without a future identifiability constraint and assay metadata.

## EditFlow / Path-OLD

The active methods direction is budgeted, path-aware distillation of an expensive
scientific oracle over a discrete edit graph. The target claim is not that edge
loss is new; it is path-aware, query-efficient oracle distillation with explicit
acquisition/generalization separation.

Permanent evaluation rules:

- report acquisition regret, held-out novel-design regret, and campaign regret
  separately;
- objective comparisons use identical queried node IDs;
- acquisition comparisons use identical initial sets, candidate universes,
  unique-node budgets, and cost models, while acquired IDs may differ;
- GB1 has become a development landscape and is not a blind test;
- FLIP2/TrpB test rows have not been downloaded or inspected.

GB1 random, greedy, UCB, Thompson, and occupancy-only development baselines have
completed on GPU 6 and are consolidated in
`analysis/editflow_gb1_standard_acquisition_development_matrix_v1.md`. Their
radius-2 normalized novel-design regret AUCs are 2.8035 (random), 2.0757
(greedy), 1.9502 (UCB), 2.0416 (Thompson), and 1.8662 (occupancy). Occupancy is
strongest locally while UCB is more competitive at radius 4; this is development
evidence for an exploitation/coverage trade-off, not a positive adaptive-method
result. The hybrid and uncertainty v2 final-checkpoint regret audits
completed; the path audit stopped after anchor 9 and must be rerun if that
comparison is needed. Do not turn post-hoc cells into confirmatory claims.

Adaptive development code now records normalized path entropy, effective path
support, and ensemble endpoint consensus, and deterministically allocates the
remaining budget to UCB exploration. The conformal edge-error utility requires
held-out or cross-fitted residuals by contract; raw ensemble uncertainty is not
claimed to upper-bound edit-field error.

The first complete adaptive path/UCB development run is
`outputs/editflow_gb1_adaptive_path_ucb_development_v1+09-01-14-49`. It is not a
headline win. Final global R-squared was 0.4743, but radius-2 normalized
novel-design regret AUC was 2.2114, worse than occupancy-only (1.8662) and UCB
(1.9502). Final radius-4 campaign regret was 0.9940, between UCB (0.7233) and
occupancy-only (1.8637). Realized path allocation ranged 0.369--0.559 with mean
0.487, showing that the first entropy/endpoint rule remained close to a 50/50
portfolio. Do not claim it solves adaptive allocation; prioritize cross-fitted
edge-error calibration rather than fraction sweeps.

The next prequential-calibrated path candidate completed on the same 16 GB1
development anchors. It uses only predictions made before a frontier target was
purchased to calibrate later rounds. Radius-2 novel-design regret AUC is 1.9816:
better than the first adaptive candidate (2.2114), but worse than UCB (1.9502)
and occupancy-only (1.8662). Final edge Spearman is the current best at 0.3671,
and final radius-2 campaign regret is 0.0255, but these secondary endpoints do
not rescue the primary curve-level null. Additive corrections of roughly
1.20--1.49 saturate toward a support-limited occupancy/UCB hybrid. Do not open
an untouched landscape on this evidence; see
`analysis/editflow_gb1_prequential_calibrated_path_development_v1.md`.

The PLS mutation PoC contains 24 train/validation anchors, 384 single-mutant
edges, and 408 exact sequence queries. Mean ESM-2 embeddings are complete.
Exact-mutant ESMFold is now running through the authorized local GPUs 0--3;
GPUs 4--7 host an unrelated high-memory service and must not be altered. A second,
non-destructive copy of the oracle code/data is staged under
`husrcf@star:/data/husrcf/PLS/editflow_oracle_20260901`. ESMFold checkpoint and
manifest hashes match local sources. Star is Slurm-managed and has two A100 80GB
GPUs; never bind its login-node devices directly. The queued star dependency
chain (jobs 60, 62, 64, and 73--77) was cancelled without losing results on
2026-09-01 because every job was still pending and the authorized local GPUs
were substantially faster. Star now only holds a verified fallback copy; there
are no active project Slurm jobs. Remote V4 prerequisites `freesasa==2.2.1` and
`pydssp==0.9.1` were installed only after clean dry-run resolutions followed by
`--no-build-isolation` installs.
On a train anchor, the star re-extraction exactly matched every frozen tensor
(62D physicochemical, 89D spatial scalar, 8x3 vector, coordinates, and pLDDT;
all maximum absolute differences were zero). No test entity was used.

The local machine became the primary execution path on 2026-09-01 after GPUs
0--3 were explicitly authorized. The missing internal ESM2-3B checkpoint is now
present. Official ESMFold loading uses an audited compatibility shim: exactly
four OpenFold IPA projection keys are remapped and exactly five known legacy
buffers are ignored and recorded; any other mismatch hard-fails. GPUs 0--3 run
shards 0/4, 1/5, 2/6, and 3/7 respectively. The first four shards completed and
all 384 exact mutant structures completed by 15:18 with zero failures. The
automated post-fold pipeline then completed V4 caches, exact residue ESM2/PCA,
coherent teacher scoring, and the value-KD student by 15:20. Stage transitions
are timestamped in
`artifacts/oracles/pls_editflow_poc_v1/logs/local_postfold_pipeline.log`.

Canonical PLS oracle scoring is the matched float32 artifact
`scores_matched_fp32_a`; immediate replay differed by at most `2.87e-6`, versus
BF16 replay drift up to `0.053`. The same-checkpoint sequence ablation confirms
a large structural/fusion contribution: across 384 edges, mean absolute full,
sequence-only, and residual deltas are 0.2090, 0.0487, and 0.1911. The first
same-node PLS student control is null for edge loss: Value KD versus edge KD has
edge Spearman 0.1171 versus 0.0777 and sign accuracy 0.5391 versus 0.5312. See
`analysis/editflow_pls_oracle_poc_v1.md` and
`analysis/editflow_pls_student_poc_v1.md`. No test entity was used.
The structure signal persists on 17 high-confidence anchors (parent mean pLDDT
at least 0.7): mean absolute residual delta is 0.1485 versus sequence-only delta
0.0361. Medium-confidence anchors are the least stable and must remain a
separate sensitivity stratum.
The first frozen-ESM2 student experiment is also null on global mutation ranking.
PLM potential improves edge Pearson/RMSE/sign to 0.6831/0.5383/0.6016 but edge
Spearman remains 0.1028; parent-state direct delta and cycle delta obtain 0.0856
and 0.0743. The cycle model uses 1,900 commuting squares and zero extra teacher
labels. Train-anchor Group-CV ridge diagnostics are also null, so nonlinear-head
overfitting is not the sole cause. These 272 training nodes represent only 16
independent proteins; see `analysis/editflow_pls_plm_student_poc_v1.md`.

The 128-train/32-validation scaled oracle completed with zero fold failures.
Across 2,560 mutation edges, mean absolute full, same-checkpoint sequence, and
structure/fusion-residual deltas are 0.2275, 0.0471, and 0.2055. Frozen-PLM
potential reaches edge Spearman 0.3297 versus raw-token 0.1283. Parent-only
direct delta and cycle delta remain null (0.0795/0.0822); exact-pair delta reaches
0.2887. The strongest learned student is matched sequence plus a learned
structural-residual potential: edge Pearson 0.4159, Spearman 0.3727, sign 0.6563,
and RMSE 0.6208. It improves magnitude fidelity but does not beat the matched
sequence branch's rank/sign fidelity (0.4279/0.6855). A second float32 teacher
replay differs by at most 1.93e-4 in full logits (mean 4.54e-7) and is bit-exact
for sequence-only logits. See `analysis/editflow_pls_student_scale_v1.md`.

A post-hoc audit found that the 128 train entities cover only 89 SI30 components.
The completed 1k protocol fixes that design flaw: exactly 1,024 unique train
components and 128 unique validation components, 16 mutations per anchor,
18,432 edges, and 19,584 exact nodes. A complete 2,560-edge ablation showed that
zero-refold parent-structure reuse plus exact mutant PLM tokens preserves the
exact-refold field surprisingly well: all-edge Spearman 0.6967, sign 0.7875,
top-5 recall 0.6950; validation values are 0.7692/0.8281/0.7125. Node Pearson is
0.9967. Exhaustive folding is therefore paused and superseded by a multi-fidelity
protocol. Dense fixed-parent inference covers all 18,432 edges; exact folding was
restricted to one edge per train component plus all 16 validation edges (3,072
total, 822 cached, 2,250 new), an 86.4% reduction in remaining fold work. Four
balanced exact-calibration shards completed on GPUs 0--3 with zero failures.
`pls_1k_multifidelity` built exact/fixed V4 caches, extracted full exact-sequence
PLM features once, reindexed the exact subset by sequence hash, and scored both
coherent float32 oracles. Test query count remains zero.

The first frozen correction experiment is null. Five-fold grouped OOF selection
on 1,024 component-unique train edges retained raw fixed-parent over affine,
ridge-residual, and nonlinear-residual correction. On 2,048 exact validation
edges, fixed-parent obtains Pearson/Spearman `0.7481/0.6896`, sign `0.7842`, RMSE
`0.3414`, macro Kendall `0.5850`, and top-5 recall `0.6844`. Affine is effectively
unchanged; ridge/nonlinear reduce rank fidelity. The discrepancy predictor has
validation Spearman `0.2013`, and selective refolding does not beat random at
1--20% exact-fold fractions. Do not claim sparse correction or selective
refolding works; only the strong cached-backbone local oracle is established.
See `analysis/editflow_pls_multifidelity_correction_v1.md`.

Decision-focused follow-up changes the target from residual magnitude to exact
choice. On the frozen 128-component validation report, Top-M exact verification
finds the exact best mutation with probabilities `0.4922/0.6641/0.8438/0.9531`
for M=`1/2/4/8`; mean regret is `0.1338/0.0790/0.0404/0.0073`. Protein-specific
1/2/4-probe shrinkage slopes do not improve held-out RMSE, so per-anchor scalar
attenuation is also null. Train-only simultaneous residual envelopes require
about 15/16 refolds. A component-safe conformal rank set needs 12/16 for 96.1%
cross-fit coverage; the preferred variable margin set reaches 92.2% coverage at
10.07/16 mean queries. Its alpha `0.1` and margin `0.2325940132` are frozen.

A new confirmatory manifest contains 64 label-blind train-only anchors from 64
SI30 components excluded from all 1,152 prior oracle components, with 16 edits
per anchor. All 1,024 exact folds completed on physical GPUs 0--3 with zero
failures, using 3,060 GPU-seconds and 891 seconds parallel wall time. The frozen
margin method achieved 60/64 exact-best coverage (`0.9375`, two-sided 95%
Clopper--Pearson `[0.8476,0.9827]`) at mean 9.70/16 queries (`0.6064` cost) and
mean regret `0.0271`. Top-4/8 achieved `0.8281/0.9375` coverage with mean regret
`0.1107/0.0952`; rank-12 achieved `0.9844` coverage and `0.0204` mean regret.
Four margin-set misses remain, including one regret `1.3061`, so this is a
marginal risk-control result, not a deterministic certificate. This did not use
the repository PLS test split. See
`analysis/editflow_pls_decision_gating_development_v1.md` and
`analysis/editflow_pls_decision_gating_confirmatory_v1.md`.
The complete small 382 MiB oracle artifact tree was copied non-destructively to
the star staging root earlier; star currently has no active project Slurm jobs.

## Operational contracts

- Current local authorization is physical GPUs 0--3; GPUs 4--7 remain occupied
  by an unrelated high-memory service and must not be altered. On star, GPU access must go
  through Slurm GRES; never bind the login-node devices directly.
- Training runs live under `outputs/<experiment>+MM-DD-HH-MM/` with config,
  environment, TensorBoard, logs, and aggregate validation metrics.
- Checkpoints and raw prediction arrays are excluded from Git.
- Before any Python installation, run pip dry-run, inspect dependency changes,
  then install with `--no-build-isolation`; abort on core-stack conflicts.
- Preserve unrelated dirty/untracked user outputs.
- Current full suite after decision-gating support: 157 tests ran,
  all successful, with 1 authorized-GPU smoke test skipped.

## Next permitted work

1. Administrative freeze verification, manuscript figures/tables, and provenance
   audits for core PLS.
2. EditFlow acquisition baselines, regret decomposition, calibrated edge-error
   research, and external non-test landscape protocol design.
3. Treat the completed 1k exact validation report as development evidence. Any
   next correction/selective-refolding design must be chosen from train grouped
   OOF evidence or a newly frozen non-test protocol; do not iterate on the same
   2,048 validation edges and do not open test.

Do not resume core PLS validation optimization or evaluate any test split.
